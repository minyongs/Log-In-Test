server :
  port : 8081

spring :
  datasource:
    driver-class-name : com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/db_song?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username : user_song
    password : 5205
    thymeleaf:
    cache : false

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    open-in-view: false
    show-sql: true
    hibernate:
      ddl-auto: update